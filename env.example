# Copy this file to .env.local and customize values as needed

# Required to customize which GitHub profile is displayed
GITHUB_USERNAME=your_github_username

# Optional: personal access token for GitHub GraphQL API (higher rate limits and richer data)
# Without this token, the app falls back to a public pinned-repos service
# GITHUB_TOKEN=your_github_pat
